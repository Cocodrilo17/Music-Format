# Music-Format

A lightweight domain-specific language (DSL) for cataloging and searching music designed to be fast to write, readable in plain text, and compilable to JSON and HTML.

Built from scratch with a full toolchain: compiler, VSCode extension with syntax highlighting, linter, and autocomplete.
